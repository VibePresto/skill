interface:
  display_name: "VibePresto"
  short_description: "Upload single-page bundles to WordPress"
  default_prompt: "Use $vibepresto with the config-backed `npx vibepresto` CLI to build, verify, and upload one static page bundle to a VibePresto WordPress page."
